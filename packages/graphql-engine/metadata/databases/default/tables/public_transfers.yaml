table:
  name: transfers
  schema: public
object_relationships:
  - name: block
    using:
      manual_configuration:
        column_mapping:
          blockNumber: blockNumber
        insertion_order: null
        remote_table:
          name: blocks
          schema: public
