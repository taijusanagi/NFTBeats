table:
  name: transfers
  schema: public
object_relationships:
  - name: Block
    using:
      manual_configuration:
        column_mapping:
          blockNumber: blockNumber
        insertion_order: null
        remote_table:
          name: syncedBlocks
          schema: public
