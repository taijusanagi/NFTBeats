table:
  name: blocks
  schema: public
array_relationships:
  - name: transactions
    using:
      manual_configuration:
        column_mapping:
          blockNumber: blockNumber
        insertion_order: null
        remote_table:
          name: transactions
          schema: public
